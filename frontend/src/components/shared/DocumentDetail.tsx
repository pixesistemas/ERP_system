import { useState, useEffect } from "react";
import { api } from "../../services/api";

export function DocumentDetail({id}:{id:number}){const [doc,setDoc]=useState<any>(null);useEffect(()=>{api.getDocument(id).then(r=>setDoc(r.documento||r)).catch(()=>{})},[id]);const items=doc?.items||doc?.detalles||[];const itemTotal=(i:any)=>{const neto=i.subtotal!=null?Number(i.subtotal):Number(i.cantidad||0)*Number(i.precio_unitario||i.precio||0);const tieneIva=Number(i.total||0)!==neto;return tieneIva?Number(i.total):neto};return <table className="inner-detail"><thead><tr><th>Código</th><th>Descripción</th><th>Cantidad</th><th>Precio</th><th>Subtotal</th></tr></thead><tbody>{items.map((i:any,n:number)=><tr key={n}><td>{i.codigo||'-'}</td><td>{i.descripcion||i.detalle}</td><td>{i.cantidad}</td><td>$ {(itemTotal(i)/(Number(i.cantidad)||1)).toLocaleString('es-AR',{minimumFractionDigits:2})}</td><td>$ {itemTotal(i).toLocaleString('es-AR',{minimumFractionDigits:2})}</td></tr>)}</tbody></table>}