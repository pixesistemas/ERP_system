import { useState } from "react";
import { Plus, Pencil, Save, X } from "lucide-react";
import { useServerRows } from "../../hooks/useServerStorage";

export function SellersPage(){const [rows,save]=useServerRows('afip_sellers_v31',[]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save(editing.id?rows.map((r:any)=>r.id===editing.id?editing:r):[{...editing,id:Date.now()},...rows]);setEditing(null)}return <div className="products-page"><div className="products-toolbar"><div><h3>Vendedores y comisiones</h3><p>Asignación obligatoria para notas de pedido y reportes por período.</p></div><button className="primary-action" onClick={()=>setEditing({nombre:'',comision:0,activo:true})}><Plus/> Nuevo vendedor</button></div><div className="products-card"><table><thead><tr><th>Vendedor</th><th>Comisión</th><th>Estado</th><th></th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.nombre}</strong></td><td>{r.comision}%</td><td><span className={r.activo?'badge success':'badge danger'}>{r.activo?'ACTIVO':'INACTIVO'}</span></td><td><button onClick={()=>setEditing({...r})}><Pencil size={16}/></button></td></tr>)}</tbody></table></div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal seller-modal" onSubmit={submit}><div className="modal-head"><div><h3>Vendedor</h3><p>Datos para pedidos y liquidación de comisiones.</p></div><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label className="full">Nombre<input autoFocus required value={editing.nombre} onChange={e=>setEditing({...editing,nombre:e.target.value})}/></label><label>Comisión %<input type="number" min="0" step="0.01" value={editing.comision} onFocus={e=>e.currentTarget.select()} onChange={e=>setEditing({...editing,comision:Number(e.target.value)})}/></label><label className="toggle-row"><span>Activo</span><input type="checkbox" checked={editing.activo} onChange={e=>setEditing({...editing,activo:e.target.checked})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save"><Save size={16}/> Guardar</button></div></form></div>}</div>}
