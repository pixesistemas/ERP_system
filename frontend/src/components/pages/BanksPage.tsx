import { useState } from "react";
import { Plus, Pencil, Save, X } from "lucide-react";
import { useServerRows } from "../../hooks/useServerStorage";

export function BanksPage(){const [rows,save]=useServerRows('afip_banks_v32',[{id:1,nombre:'Banco Nación',cbu:'',alias:'',cuenta:'',saldo:0}]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save(editing.id?rows.map((x:any)=>x.id===editing.id?editing:x):[{...editing,id:Date.now()},...rows]);setEditing(null)}return <div className="products-page"><div className="products-toolbar"><div><h3>Bancos</h3><p>Cuentas bancarias usadas en cheques, transferencias y conciliación.</p></div><button className="primary-action" onClick={()=>setEditing({nombre:'',titular:'',cuenta:'',cbu:'',alias:'',saldo:0})}><Plus/> Nuevo banco</button></div><div className="products-card"><table><thead><tr><th>Banco</th><th>Titular</th><th>Cuenta</th><th>CBU</th><th>Alias</th><th>Saldo</th><th></th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.nombre}</strong></td><td>{r.titular||"—"}</td><td>{r.cuenta}</td><td>{r.cbu}</td><td>{r.alias}</td><td className="price">$ {Number(r.saldo||0).toLocaleString('es-AR',{minimumFractionDigits:2})}</td><td><button onClick={()=>setEditing({...r})}><Pencil size={16}/></button></td></tr>)}</tbody></table></div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal" onSubmit={submit}><div className="modal-head"><h3>Cuenta bancaria</h3><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label className="full">Banco<input required autoFocus value={editing.nombre} onChange={e=>setEditing({...editing,nombre:e.target.value})}/></label><label>Titular<input value={editing.titular||""} onChange={e=>setEditing({...editing,titular:e.target.value})} placeholder="Ej: PixeSistemas S.R.L."/></label><label>Cuenta<input value={editing.cuenta} onChange={e=>setEditing({...editing,cuenta:e.target.value})}/></label><label>Saldo inicial<input type="number" value={editing.saldo} onFocus={e=>e.currentTarget.select()} onChange={e=>setEditing({...editing,saldo:Number(e.target.value)})}/></label><label>CBU<input value={editing.cbu} onChange={e=>setEditing({...editing,cbu:e.target.value})}/></label><label>Alias<input value={editing.alias} onChange={e=>setEditing({...editing,alias:e.target.value})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save"><Save size={16}/> Guardar</button></div></form></div>}</div>}
