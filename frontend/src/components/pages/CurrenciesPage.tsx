import { useState } from "react";
import { Plus, Pencil, X } from "lucide-react";
import { useServerRows } from "../../hooks/useServerStorage";

export function CurrenciesPage(){const [rows,save]=useServerRows('afip_currencies_v34',[{id:1,codigo:'PES',nombre:'Peso argentino',simbolo:'$',cotizacion:1,principal:true},{id:2,codigo:'DOL',nombre:'Dólar estadounidense',simbolo:'US$',cotizacion:1,principal:false}]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save(rows.some((x:any)=>x.id===editing.id)?rows.map((x:any)=>x.id===editing.id?editing:x):[{...editing,id:Date.now()},...rows]);setEditing(null)}return <div className="products-page"><div className="products-toolbar"><div><h3>Monedas y cotizaciones</h3><p>Configuración multimoneda para compras, ventas y comprobantes.</p></div><button className="primary-action" onClick={()=>setEditing({codigo:'',nombre:'',simbolo:'',cotizacion:1,principal:false})}><Plus/> Nueva moneda</button></div><div className="products-card"><table><thead><tr><th>Código ARCA</th><th>Moneda</th><th>Símbolo</th><th>Cotización</th><th>Principal</th><th></th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.codigo}</strong></td><td>{r.nombre}</td><td>{r.simbolo}</td><td>{r.cotizacion}</td><td>{r.principal?'Sí':'No'}</td><td><button onClick={()=>setEditing(r)}><Pencil size={16}/></button></td></tr>)}</tbody></table></div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal" onSubmit={submit}><div className="modal-head"><h3>Moneda</h3><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label>Código<input required value={editing.codigo} onChange={e=>setEditing({...editing,codigo:e.target.value.toUpperCase()})}/></label><label>Nombre<input required value={editing.nombre} onChange={e=>setEditing({...editing,nombre:e.target.value})}/></label><label>Símbolo<input value={editing.simbolo} onChange={e=>setEditing({...editing,simbolo:e.target.value})}/></label><label>Cotización<input type="number" step="0.000001" value={editing.cotizacion} onChange={e=>setEditing({...editing,cotizacion:Number(e.target.value)})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save">Guardar</button></div></form></div>}</div>}
