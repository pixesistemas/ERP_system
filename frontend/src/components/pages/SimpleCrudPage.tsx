import { useState } from "react";
import { Plus, Pencil, X } from "lucide-react";
import { useServerRows } from "../../hooks/useServerStorage";

export function SimpleCrudPage({kind}:{kind:'SUCURSALES'|'CAJEROS'}){const key=kind==='SUCURSALES'?'afip_branches_v32':'afip_cashiers_v32';const [rows,save]=useServerRows(key,[]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save(editing.id?rows.map((x:any)=>x.id===editing.id?editing:x):[{...editing,id:Date.now(),activo:true},...rows]);setEditing(null)}return <div className="products-page"><div className="products-toolbar"><div><h3>{kind==='SUCURSALES'?'Sucursales':'Cajeros'}</h3><p>{kind==='SUCURSALES'?'Locales y depósitos operativos.':'Usuarios operativos para ventas y reportes.'}</p></div><button className="primary-action" onClick={()=>setEditing({nombre:'',codigo:'',domicilio:'',activo:true})}><Plus/> Nuevo</button></div><div className="products-card"><table><thead><tr><th>Código</th><th>Nombre</th><th>Domicilio</th><th>Estado</th><th></th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.codigo}</td><td><strong>{r.nombre}</strong></td><td>{r.domicilio}</td><td>{r.activo?'ACTIVO':'INACTIVO'}</td><td><button onClick={()=>setEditing({...r})}><Pencil size={16}/></button></td></tr>)}</tbody></table></div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal" onSubmit={submit}><div className="modal-head"><h3>{kind==='SUCURSALES'?'Sucursal':'Cajero'}</h3><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label>Código<input value={editing.codigo} onChange={e=>setEditing({...editing,codigo:e.target.value})}/></label><label>Nombre<input required value={editing.nombre} onChange={e=>setEditing({...editing,nombre:e.target.value})}/></label><label className="full">Domicilio<input value={editing.domicilio} onChange={e=>setEditing({...editing,domicilio:e.target.value})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save">Guardar</button></div></form></div>}</div>}
