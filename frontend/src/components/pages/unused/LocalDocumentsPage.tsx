import { useState } from "react";
import { Search } from "lucide-react";
import { useServerRows } from "../../../hooks/useServerStorage";

export function LocalDocumentsPage({tipo}:{tipo:string}){const [q,setQ]=useState('');const [rows]=useServerRows('afip_pos_documents');const filtered=rows.filter((r:any)=>String(r.tipo).includes(tipo)&&`${r.cliente} ${r.numero} ${r.tipo}`.toLowerCase().includes(q.toLowerCase()));return <div className="products-page"><div className="products-toolbar"><div><h3>{tipo==='RESERVA'?'Reservas':'Notas de venta'}</h3><p>Documentos registrados desde el punto de venta.</p></div></div><div className="search-box"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar cliente o número..."/></div><div className="products-card"><table><thead><tr><th>Fecha</th><th>Número</th><th>Cliente</th><th>Estado</th><th>Total</th><th>Ítems</th></tr></thead><tbody>{filtered.map((r:any)=><tr key={r.id}><td>{new Date(r.fecha).toLocaleString('es-AR')}</td><td><strong>{r.numero||r.id}</strong></td><td>{r.cliente}</td><td>{r.estado}</td><td className="price">$ {Number(r.total).toLocaleString('es-AR',{minimumFractionDigits:2})}</td><td>{r.items?.length||0}</td></tr>)}</tbody></table>{!filtered.length&&<div className="empty-table">No hay documentos para mostrar.</div>}</div></div>}
