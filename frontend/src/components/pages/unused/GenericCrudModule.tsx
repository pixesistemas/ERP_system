import { useState } from "react";
import { Plus, Pencil, X } from "lucide-react";
import { useServerRows } from "../../../hooks/useServerStorage";

export function GenericCrudModule({kind}:{kind:string}){const [rows,save]=useServerRows(`afip_${kind.toLowerCase()}_v33`,[]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save(editing.id?rows.map((x:any)=>x.id===editing.id?editing:x):[{...editing,id:Date.now(),activo:true},...rows]);setEditing(null)}const labels:any={COMBOS:'Combos de productos',DESCUENTOS:'Descuentos por cantidad',PROMOCIONES:'Promociones y regalos',SORTEOS:'Cupones de sorteo'};return <div className="products-page"><div className="products-toolbar"><div><h3>{labels[kind]}</h3><p>Reglas comerciales configurables para el punto de venta.</p></div><button className="primary-action" onClick={()=>setEditing({nombre:'',condicion:'',beneficio:'',vigenciaDesde:new Date().toISOString().slice(0,10),vigenciaHasta:'',activo:true})}><Plus/> Nueva regla</button></div><div className="products-card"><table><thead><tr><th>Nombre</th><th>Condición</th><th>Beneficio</th><th>Vigencia</th><th>Estado</th><th></th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.nombre}</strong></td><td>{r.condicion}</td><td>{r.beneficio}</td><td>{r.vigenciaDesde} — {r.vigenciaHasta||'Sin límite'}</td><td>{r.activo?'ACTIVO':'INACTIVO'}</td><td><button onClick={()=>setEditing({...r})}><Pencil size={16}/></button></td></tr>)}</tbody></table>{!rows.length&&<div className="empty-table">No hay reglas configuradas.</div>}</div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal" onSubmit={submit}><div className="modal-head"><h3>{labels[kind]}</h3><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label className="full">Nombre<input required value={editing.nombre} onChange={e=>setEditing({...editing,nombre:e.target.value})}/></label><label className="full">Condición<input required value={editing.condicion} placeholder="Ej.: desde 10 unidades o compra mayor a $50.000" onChange={e=>setEditing({...editing,condicion:e.target.value})}/></label><label className="full">Beneficio<input required value={editing.beneficio} placeholder="Ej.: 8% descuento, producto gratis o cupón" onChange={e=>setEditing({...editing,beneficio:e.target.value})}/></label><label>Desde<input type="date" value={editing.vigenciaDesde} onChange={e=>setEditing({...editing,vigenciaDesde:e.target.value})}/></label><label>Hasta<input type="date" value={editing.vigenciaHasta} onChange={e=>setEditing({...editing,vigenciaHasta:e.target.value})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save">Guardar</button></div></form></div>}</div>}
