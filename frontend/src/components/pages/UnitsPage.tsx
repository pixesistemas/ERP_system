import { useState } from "react";
import { Plus, Pencil, Trash2, X } from "lucide-react";
import { useServerRows } from "../../hooks/useServerStorage";

export function UnitsPage(){const [rows,save]=useServerRows('afip_units_v34',[{id:1,codigo:'UN',nombre:'Unidad',decimales:0},{id:2,codigo:'KG',nombre:'Kilogramo',decimales:3},{id:3,codigo:'M3',nombre:'Metro cúbico',decimales:3}]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save(rows.some((x:any)=>x.id===editing.id)?rows.map((x:any)=>x.id===editing.id?editing:x):[{...editing,id:Date.now()},...rows]);setEditing(null)}return <div className="products-page"><div className="products-toolbar"><div><h3>Unidades de medida</h3><p>Unidades disponibles para productos y comprobantes.</p></div><button className="primary-action" onClick={()=>setEditing({codigo:'',nombre:'',decimales:0})}><Plus/> Nueva unidad</button></div><div className="products-card"><table><thead><tr><th>Código</th><th>Descripción</th><th>Decimales</th><th></th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><strong>{r.codigo}</strong></td><td>{r.nombre}</td><td>{r.decimales}</td><td><button onClick={()=>setEditing(r)}><Pencil size={16}/></button> <button onClick={()=>save(rows.filter((x:any)=>x.id!==r.id))}><Trash2 size={16}/></button></td></tr>)}</tbody></table></div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal" onSubmit={submit}><div className="modal-head"><h3>Unidad de medida</h3><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label>Código<input required value={editing.codigo} onChange={e=>setEditing({...editing,codigo:e.target.value.toUpperCase()})}/></label><label>Nombre<input required value={editing.nombre} onChange={e=>setEditing({...editing,nombre:e.target.value})}/></label><label>Decimales<input type="number" min="0" max="6" value={editing.decimales} onChange={e=>setEditing({...editing,decimales:Number(e.target.value)})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save">Guardar</button></div></form></div>}</div>}
