import { useState } from "react";
import { Plus, X } from "lucide-react";
import { useServerRows } from "../../hooks/useServerStorage";

export function ProductSuppliersPage(){const [rows,save]=useServerRows('afip_product_suppliers_v33',[]);const [editing,setEditing]=useState<any>(null);function submit(e:any){e.preventDefault();save([{...editing,id:Date.now()},...rows]);setEditing(null)}return <div className="products-page"><div className="products-toolbar"><div><h3>Proveedores por producto</h3><p>Un producto puede tener varios proveedores, costos y códigos propios.</p></div><button className="primary-action" onClick={()=>setEditing({producto:'',proveedor:'',codigoProveedor:'',costo:0,principal:false})}><Plus/> Asociar</button></div><div className="products-card"><table><thead><tr><th>Producto</th><th>Proveedor</th><th>Código proveedor</th><th>Costo</th><th>Principal</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.producto}</td><td>{r.proveedor}</td><td>{r.codigoProveedor}</td><td className="price">$ {Number(r.costo).toLocaleString('es-AR',{minimumFractionDigits:2})}</td><td>{r.principal?'SÍ':'NO'}</td></tr>)}</tbody></table></div>{editing&&<div className="modal-backdrop"><form className="product-modal polished-modal" onSubmit={submit}><div className="modal-head"><h3>Asociar proveedor</h3><button type="button" onClick={()=>setEditing(null)}><X/></button></div><div className="form-grid"><label>Producto<input required value={editing.producto} onChange={e=>setEditing({...editing,producto:e.target.value})}/></label><label>Proveedor<input required value={editing.proveedor} onChange={e=>setEditing({...editing,proveedor:e.target.value})}/></label><label>Código proveedor<input value={editing.codigoProveedor} onChange={e=>setEditing({...editing,codigoProveedor:e.target.value})}/></label><label>Costo<input type="number" value={editing.costo} onChange={e=>setEditing({...editing,costo:Number(e.target.value)})}/></label><label className="toggle-row"><span>Proveedor principal</span><input type="checkbox" checked={editing.principal} onChange={e=>setEditing({...editing,principal:e.target.checked})}/></label></div><div className="modal-actions"><button type="button" onClick={()=>setEditing(null)}>Cancelar</button><button className="save">Guardar</button></div></form></div>}</div>}
